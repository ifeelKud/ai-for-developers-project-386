{
  "name": "booking-service-spec",
  "version": "1.0.0",
  "description": "TypeSpec specification for Booking Service API",
  "type": "module",
  "scripts": {
    "compile": "tsp compile spec/booking.tsp --emit @typespec/openapi3",
    "watch": "tsp compile spec/booking.tsp --emit @typespec/openapi3 --watch"
  },
  "dependencies": {},
  "devDependencies": {
    "@typespec/http": "latest",
    "@typespec/rest": "latest",
    "@typespec/openapi3": "latest",
    "@typespec/compiler": "latest",
    "typescript": "latest"
  }
}